# Configure these in Vercel Project Settings > Environment Variables.
# Keep both values server-side. Do not add them to a Vite environment variable.
GOOGLE_SHEETS_WEB_APP_URL=https://script.google.com/macros/s/your-deployment-id/exec
GOOGLE_SHEETS_FORM_TOKEN=replace-with-a-long-random-secret
